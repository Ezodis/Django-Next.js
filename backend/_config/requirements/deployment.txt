# backend/_config/requirements/deployment.txt
# Production-specific dependencies

-r base.txt

# WSGI Server
gunicorn==23.0.0

# Production database driver (compiled, more performant)
# Uncomment and remove psycopg2-binary from base.txt for production
# psycopg2==2.9.9

# Monitoring & Error Tracking
sentry-sdk==1.40.0  # Error tracking and performance monitoring

# Security
django-csp==3.8  # Content Security Policy headers
django-permissions-policy==4.19.0  # Permissions-Policy header

# Performance
django-cachalot==2.6.1  # Automatic caching for ORM queries (optional)

# Health checks
django-health-check==3.18.1  # Health check endpoints for load balancers